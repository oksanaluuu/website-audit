# Optional: Google PageSpeed Insights API key (free, but rate-limited without key)
# Get one at: https://developers.google.com/speed/docs/insights/v5/get-started
# Without a key it still works, just slower (anonymous quota)
PAGESPEED_API_KEY=

# Your agency name — appears in report header and footer
AGENCY_NAME=Your Agency Name
